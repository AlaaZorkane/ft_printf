%[flags][width][.precision][length]type

TODO:
    - negative values with asteriks (FLAG minus / ignore in precision)


Working:
    - width > precision
    - width < precision
    - width alone
    - precision alone
    - minus flag with width alone
    - minus flag with 0 flag and width alone
    - minus flag with 0 flag and width > precision
    - minus flag with 0 flag and width < precision
